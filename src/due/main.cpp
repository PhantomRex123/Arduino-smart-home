#include <Arduino.h>
#include <Adafruit_Sensor.h>

void setup()
{
    Serial.begin(115200);
    // servo.attach(SERVO_INPUT);
    // dht.begin();
    // pinMode(MQ2_INPUT_DIGITAL, INPUT);
    // irrecv.enableIRIn();
    // irrecv.blink13(true);
}
